import React, {useContext} from 'react';
import { Redirect } from 'react-router-dom';

const Login = ({ history }) => {
        return (
            <div>
                <h1>Login page</h1>
            </div>
        )
};

export default Login;